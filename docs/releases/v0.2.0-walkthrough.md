# v0.2.0 release walkthrough

Status legend: ✅ = already done by Claude this session; 👤 = your step.

## Already done ✅

1. **M4 merged to main** (merge commit on top of `5fb74ac`), version bumped to
   `0.2.0` (`87b230a`), tag `v0.2.0` created and pushed.
2. **Release archive built** with the engine's own packager
   (`go run . module package weather`) from the synced checkout copy:
   - File: `C:\Users\Calabe Davis\workspace\release-v0.2.0\weather.tar.gz`
   - SHA256: `67ee23db2a5b947b1d5ceb69378dec0b6fc77d992ac22e48ef902293553ae1f6`
   - Layout verified: single `weather/` wrapper dir, 136 entries, no dev files
     (no go.mod, docs, scripts, .github, worktrees).
3. **CI** triggered by the push to main — first-ever run of the new workflow:
   <https://github.com/GoMudEngine/GoMud-Module-Weather/actions>
   Both jobs must go green before you publish the release (the engine-coupled
   job tracks upstream master; if it alone is red, upstream moved — ping me).

## Step 1 👤 — Create the GitHub release

1. Open: <https://github.com/GoMudEngine/GoMud-Module-Weather/releases/new?tag=v0.2.0>
   (the tag already exists — pick `v0.2.0` if it isn't preselected).
2. Release title:
   ```
   v0.2.0 — Seasons, admin page, per-room weather
   ```
3. Body: paste the whole of [`v0.2.0-release-notes.md`](v0.2.0-release-notes.md).
4. Attach the asset: drag in
   `C:\Users\Calabe Davis\workspace\release-v0.2.0\weather.tar.gz`
   (the filename must stay exactly `weather.tar.gz` — the registry URL points
   at it).
5. Publish release.

Sanity check after publishing: the asset URL must be exactly
`https://github.com/GoMudEngine/GoMud-Module-Weather/releases/download/v0.2.0/weather.tar.gz`

## Step 2 👤 — Registry PR

Everything to paste (YAML block, PR title, PR body) is in
[`v0.2.0-registry-pr.md`](v0.2.0-registry-pr.md).

1. Open the web editor:
   <https://github.com/GoMudEngine/GoMud-Modules/edit/main/module-registry.yaml>
2. Replace the existing `- name: weather` entry (last in the file) with the
   block from the kit.
3. "Commit changes…" → create a new branch → open the PR with the title and
   body from the kit.

## Step 3 — After the registry PR merges

Tell me and I'll verify the full installer path end-to-end locally
(`go run . module install weather` against the live registry, hash check
included) and update the project memory/status notes.

## Separate follow-up (not part of this release)

The upstream GoMud PR for the three engine issues we've banked:
1. Login-kick nil panic (`internal/inputhandlers/login.go:35-37`) — already
   reported to Discord.
2. `configs.OverlayOverrides` replace-not-merge clobbers operator
   `Modules.<name>` config blocks (found by the M4 smoke; module self-heals).
3. `PluginConfig.Set` swallows `SetVal` errors (admin writes silently
   dropped; module now read-back-verifies).

Say the word when you want that PR drafted — it goes to the GoMud repo on its
own, never mixed with module changes, per their CONTRIBUTING.
